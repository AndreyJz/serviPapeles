const boton = document.querySelectorAll(".button");

boton.addEventListener('click', (e) =>{

});

function identify(boton){
    boton.forEach(btnId =>{
        const priceTable = document.createElement("div");
        if (btnId.id === adh){
            priceTable.className = "card";
            priceTable.innerHTML = html`
            
            `
        }
    })
}

function adh(a){
    
}